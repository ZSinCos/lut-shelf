# LUT 实时预览与选择工具 — 需求分析文档

## 1. 项目背景

用户拥有大量 `.vlt` 格式的 LUT（Look-Up Table）文件（目前约 24 个，存放于 `S5II推荐LUT/` 目录），需要一个高效的工具来**实时预览**不同 LUT 应用到图片/视频上的效果，以便快速挑选合适的 LUT。

## 2. 目标

开发一个本地运行的 Web 应用（或桌面软件），能够：

- 加载指定目录下的 LUT 文件
- 加载用户提供的参考图片/视频
- 实时切换并预览不同 LUT 的效果
- 支持对比（原始 vs LUT、多 LUT 并排等）
- 方便用户快速筛选和选择心仪的 LUT

## 3. 用户需求

### 3.1 核心功能（P0 — 必须实现）

| 功能 | 描述 |
|------|------|
| LUT 加载 | 自动扫描指定目录，加载 `.vlt` / `.cube` 等格式的 LUT 文件 |
| 图片加载 | 用户可上传或拖入参考图片（JPG/PNG/WebP） |
| 实时预览 | 将 LUT 应用到图片上，切换 LUT 时即时更新画面 |
| 列表展示 | 以列表或网格形式展示所有可用 LUT，显示文件名 |
| 快速切换 | 点击 LUT 名称即可切换预览，支持键盘快捷键（上下箭头） |

### 3.2 重要功能（P1 — 强烈建议）

| 功能 | 描述 |
|------|------|
| 对比模式 | 原始图 vs LUT 效果 左右/上下对比，或点击/悬停切换 |
| 多 LUT 对比 | 同时选择 2~4 个 LUT 分屏对比 |
| 缩放/平移 | 对图片进行缩放和拖拽，查看局部细节 |
| 搜索筛选 | 按关键词搜索 LUT 名称 |
| 视频支持 | 加载短视频并实时应用 LUT（可选） |

### 3.3 锦上添花（P2 — 可选）

| 功能 | 描述 |
|------|------|
| 收藏/标记 | 对喜欢的 LUT 标记星标，保存偏好 |
| LUT 信息显示 | 显示 LUT 的维度、类型等元信息 |
| 批量导出 | 将选中的 LUT 批量应用到一组图片并导出 |
| 历史记录 | 保存最近使用的 LUT 列表 |
| 暗色/亮色主题 | UI 主题切换 |
| 拖拽排序 | 支持拖拽调整 LUT 列表顺序 |

## 4. 技术方案选型

### 选项 A：纯前端 Web 应用（推荐）

| 层面 | 技术 |
|------|------|
| 框架 | Vanilla JS + Canvas 或 React/Vue |
| LUT 处理 | 使用 WebGL 着色器（GPU 加速）或 Canvas 像素操作实现 3D LUT 查找 |
| 图片处理 | Canvas 2D API 逐像素应用 LUT |
| 格式支持 | `.cube`（主流）为主，`.vlt` 需解析转换 |
| 部署 | 本地 `file://` 或简单 HTTP 服务器（如 `npx serve`） |

**优点**：跨平台，无需安装，开发效率高。

### 选项 B：Electron 桌面应用

| 层面 | 技术 |
|------|------|
| 框架 | Electron + React/Vue |
| 文件读取 | Node.js `fs` 模块直接读取文件系统 |
| LUT 处理 | 同上（Canvas/WebGL） |

**优点**：更好的文件系统访问、可打包成独立 exe。

### 选项 C：Python + OpenCV 桌面应用

| 层面 | 技术 |
|------|------|
| 框架 | PyQt5 / Tkinter + OpenCV |
| LUT 处理 | OpenCV `cv2.LUT()` |
| 文件读取 | 原生 Python 文件 IO |

**优点**：OpenCV 内置 LUT 支持，性能好。

**缺点**：UI 开发较慢，分发需打包。

## 5. 推荐方案

**推荐采用选项 A（纯前端 Web 应用）**，原因：

1. 开发效率最高，迭代快
2. 跨平台，Windows/macOS/Linux 均可使用
3. 使用 WebGL 着色器可实现 GPU 加速的实时 LUT 预览
4. 现代浏览器对本地文件读取（File API）支持完善

## 6. 界面原型（文字描述）

```
+-------------------------------------------------+
|  LUT 预览工具                      设置 | 帮助  |
+-------------------------------------------------+
| 上传图片  |  [========预览区域========]          |
| [选择文件] |  [                           ]      |
|            |  [    原始 / LUT 对比       ]      |
| 拖入图片   |  [                           ]      |
|            |  [                           ]      |
+------------+------------------------------------+
|  LUT 列表（可搜索）   |  当前 LUT 信息          |
| [搜索...]             |  名称: 日系小清新       |
| ☐ 日系小清新        |  类型: .vlt              |
| ☐ 滨田英明风格      |  尺寸: 33x33x33          |
| ☐ 动漫风格  ★       |                          |
| ☐ 富士影院效果      |  [对比模式] [缩放]       |
| ☐ 富士CC            |                          |
| ☐ ...               |                          |
+-------------------------------------------------+
```

## 7. 数据流

```
LUT 文件 (.vlt/.cube)
       │
       ▼
解析 LUT → 3D LUT 数据 (RGB 查找表)
       │
       ▼
加载参考图片 → 绘制到 Canvas
       │
       ▼
逐像素查表 / WebGL 纹理查找 → 输出 LUT 效果
       │
       ▼
显示在预览区域
```

## 8. 约束与注意事项

- `.vlt` 格式需要调研并实现解析器（可能为 DaVinci Resolve 的 3D LUT 格式或基于文本的格式）
- 性能要求：切换 LUT 时应无明显卡顿（< 200ms 响应）
- 本地运行，不需要网络
- 单用户工具，无需后端服务
- 内存管理：大图片（>4000px）需要考虑降采样

## 9. 里程碑

| 阶段 | 内容 | 预估工时 |
|------|------|----------|
| M1 | LUT 文件解析（.vlt / .cube） | 1 天 |
| M2 | 图片加载与 Canvas 渲染引擎 | 1 天 |
| M3 | LUT 应用到图片（CPU 版本） | 1 天 |
| M4 | UI 界面开发（LUT 列表 + 预览区） | 1 天 |
| M5 | 对比模式、搜索、交互优化 | 1 天 |
| M6 | WebGL 加速（可选优化） | 1 天 |
| M7 | 测试、完善、打包 | 0.5 天 |

总计约 **5~7 天**。

---

*文档版本：v0.1*
*创建日期：2026-07-16*
